- name: install packages on a fresh Ubuntu
  become: yes
  sudo: yes
 
  tasks:
    - name: install all packages
    apt: name={{ item }} state=present
    with_items:
    - vlc
    - audacity
    - gnupg
